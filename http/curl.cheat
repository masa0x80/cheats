% curl

# bearer token
curl -X <method> -H 'Authorization: Bearer $API_TOKEN' '<url>'

# bearer token
curl -X <method> -H 'Authorization: Bearer <token>' '<url>'

$ api_token: echo '$API_TOKEN'
$ method: echo -e 'GET\nPOST\nPUT\nDELETE\nPATCH'
